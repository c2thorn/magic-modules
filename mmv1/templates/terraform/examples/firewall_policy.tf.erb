resource "google_compute_firewall_policy" "<%= ctx[:primary_resource_id] %>" {
  provider = google-beta
  
  parent      = "organizations/<%= ctx[:test_env_vars]['org_id'] %>"
  short_name  = "<%= ctx[:vars]['policy_name'] %>"
  description = "Example Resource"
}